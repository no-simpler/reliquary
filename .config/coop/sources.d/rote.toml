# The drill nag. banner, not doctor: the banner fires the day a drill comes
# due, doctor waits out a grace, and assay already collects the latter.

[source]
id = "rote"

[source.ask]
run = ["rote", "banner", "--format", "json"]
kind = "findings"
